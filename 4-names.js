//local
const secret = "SUPER SECRET";

//shared
const john = "john";
const peter = "peter";

module.exports = { john, peter };

// {} meaning this is an Object,inside which there are two properties
// inside an object, we pass key:value pairs, but since the keyname and value
// are exactly the same here,we can pass it as is

console.log(module);
